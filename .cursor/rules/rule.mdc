---
alwaysApply: true
---

# Cursor规则配置 - 遗传算法迷宫求解器项目

## 项目定位与技术要求

你是一名资深交互可视化工程师与算法工程师。请编写可在 macOS 本地直接运行的程序，展示"遗传算法（GA）求解固定 2D 迷宫"的流畅动画。

### 非功能需求
- 深色主题、几何网格美学、柔和阴影与淡发光（glow）
- 60 FPS 目标；窗口自适应 Retina
- 代码风格统一、模块划分清晰

### 可视化规范
- 迷宫：20×20 到 100×100 可切换
- 墙用低对比度深色，路径用半透明彩色（HSL 依个体适应度映射）
- 最优解高亮（粗线+发光）
- 过渡：插值或缓动（easeInOutCubic）让世代切换平滑
- 文字：使用系统字体 San Francisco
- HUD 显示代数、最佳值、平均值、多样性指标

### 性能要求
- 确保在 Apple Silicon 上 60 FPS
- 批量绘制、尽量减少逐像素操作

---

## 版本管理规则 ⭐ 重要

**每次修改代码或功能时，必须更新版本号！**

### 版本号位置
- **Python版本**: `README.md` 中的版本历史
- **Web版本**: `web/README.md` 底部

### 版本号格式
`vX.Y.Z`
- X (主版本): 重大架构改变或不兼容更新
- Y (次版本): 新功能添加或重要优化
- Z (修订号): Bug修复或小改进

### 更新时机
- 添加新功能 → Y+1
- 修复Bug → Z+1
- 重构代码 → Y+1
- 文档更新 → Z+1（如果是重大文档更新则Y+1）

---

## 文档管理规则 ⭐⭐⭐ 新增强制规则

### 文档位置规范
1. **Python项目文档**: 统一放在 `docs/` 目录
2. **Web项目文档**: 统一放在 `web/docs/` 目录
3. **项目主文档**: `README.md` 保留在项目根目录
4. **禁止**: 在项目根目录创建临时文档（除README.md外）

### 文档提交规则（强制）
**所有文档更新（无论大小）必须提交到Git**

理由：
- 文档是项目的重要组成部分
- 保证团队成员看到最新文档
- 文档历史可追溯
- 防止文档丢失

提交示例：
```bash
git add docs/
git add web/docs/
git commit -m "docs: 添加功能说明文档"
git push
```

### 文档命名规范
- 使用大写+下划线：`FEATURE_NAME.md`（推荐用于重要文档）
- 或使用小写+连字符：`feature-name.md`（推荐用于普通文档）
- 保持命名一致性

### 文档清理规则
- 定期检查并删除过期文档
- 临时文档完成后应移动到docs/目录或删除
- 不要在根目录积累过多文档文件

---

## 项目结构规则

### Python项目结构
```
program2/
├── README.md              # 项目主文档（根目录）
├── main.py               # 唯一的启动文件
├── requirements.txt      # 依赖列表
├── .gitignore           # Git忽略规则
├── venv/                # 虚拟环境（不提交到git）
├── src/                 # 源代码目录
│   ├── __init__.py
│   ├── maze.py
│   ├── genetic_algorithm.py
│   └── visualizer.py
└── docs/                # 所有文档（提交到git）
    ├── QUICKSTART.md
    ├── INSTALL.md
    └── ...
```

### Web项目结构
```
web/
├── README.md            # Web项目主文档
├── src/                 # TypeScript源代码
│   ├── ga/             # 遗传算法模块
│   ├── vis/            # 可视化模块
│   ├── ui/             # UI组件
│   └── utils/          # 工具函数
└── docs/               # Web项目文档（提交到git）
    └── ...
```

---

## 代码规则

### 入口文件
- **Python**: `main.py` 是唯一的启动文件
- **Web**: `src/main.tsx` → `src/App.tsx`

### 核心模块
- 所有核心代码放在 `src/` 目录
- 保持模块化设计，单一职责

### 导入路径
- Python: `from src.module import Class`
- Web: `import { Class } from './module/Class'`

---

## Git提交规则

### 必须提交
- ✅ 所有源代码
- ✅ 所有文档文件（*.md）
- ✅ 配置文件（requirements.txt, package.json等）
- ✅ .gitignore

### 禁止提交
- ❌ 虚拟环境（venv/, env/, .venv/, node_modules/）
- ❌ 临时文件（__pycache__/, *.pyc, .DS_Store）
- ❌ 构建产物（dist/, build/）
- ❌ 生成的图片（*.png, *.jpg）除非是文档插图

### Commit Message规范
```
格式: <type>: <description>

类型:
- feat: 新功能
- fix: Bug修复
- docs: 文档更新
- refactor: 代码重构
- perf: 性能优化
- test: 测试相关
- chore: 构建/工具配置

示例:
docs: 添加3D可视化功能说明
feat: 实现混合遗传算法
fix: 修复迷宫生成错误
```

---

## 编码规范

### Python
- 使用UTF-8编码
- Python 3.7+
- 遵循PEP 8代码风格
- 添加类型提示
- 编写完整的docstring

### TypeScript/Web
- 使用UTF-8编码
- TypeScript严格模式
- 遵循ESLint规则
- 添加JSDoc注释
- 组件化设计

---

## 特殊注意事项

### 文档管理
- 每次创建或更新文档后**立即提交到git**
- 使用清晰的commit message
- 不要积累大量未提交的文档
- 参考：`docs/DOCUMENTATION_RULES.md`

### 版本更新
- 每次功能更新后更新README.md中的版本号
- 在版本历史中添加更新说明
- 主版本更新需要在commit message中标注

### 代码质量
- 运行前检查Lint错误
- 确保没有TypeScript类型错误
- 保持60 FPS性能目标

---

**规则版本**: v2.0  
**最后更新**: 2025-10-28  
**生效范围**: 整个项目（Python + Web）
