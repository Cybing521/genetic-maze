---
alwaysApply: true
---
你是一名资深交互可视化工程师与算法工程师。请编写可在 macOS 本地直接运行的程序，展示“遗传算法（GA）求解固定 2D 迷宫”的流畅动画。
非功能需求：深色主题、几何网格美学、柔和阴影与淡发光（glow），60 FPS 目标；窗口自适应 Retina。
动画：逐代可视化（population 线段/路径以半透明颜色叠加），显示当前最佳路径与其适应度曲线（小角落折线图），每代 200–500 个体。支持播放/暂停、单步、重置随机种子与保存 GIF/MP4。
结构：将遗传核心与渲染/UI解耦，采用可注入的 evaluate(path) 适应度函数；参数面板：交叉率、变异率、精英率、岛屿迁移间隔（可选）；提供自适应参数（基于多样性或代数）开关。
可视化规范：

迷宫：20×20 到 40×40 可切换，墙用低对比度深色，路径用半透明彩色（HSL 依个体适应度映射），最优解高亮（粗线+发光）。

过渡：插值或缓动（easeInOutCubic）让世代切换平滑。

文字：使用系统字体 San Francisco；HUD 显示代数、最佳值、平均值、多样性指标。
性能：确保在 Apple Silicon 上 60 FPS；批量绘制、尽量减少逐像素操作。
可复现性：支持 --seed 命令行参数，并在 UI 中显示 seed。
输出：给出完整可运行代码与清晰的运行步骤。严格遵循以下验收标准：

启动即有默认迷宫与运行按钮；2) 每代更新有平滑动画；3) 可一键导出动画（GIF/MP4 或 WebM）；4) 代码风格统一、模块划分清晰；5) 参数变化即时生效且不会卡顿。
